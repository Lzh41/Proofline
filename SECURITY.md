# 安全策略

## 报告安全问题

不要在公开 Issue 中提交 API 密钥、Cookie、个人数据库、平台登录信息或可利用的安全细节。请先通过 GitHub 私下联系维护者，或在确认私有联系方式后提交加密报告。

报告应包含影响范围、复现步骤、受影响版本和建议修复方向。

## 设计边界

- AI 密钥只由 Rust 写入 Windows 凭据库，不进入 SQLite、JSON 导出或 ZIP 备份。
- 官方平台窗口不拥有主窗口的 Tauri capability，不能访问本地 IPC、文件、SQLite 或凭据。
- 远程导航只允许审核后的精确 HTTPS 域名；未知域名、下载、OAuth 和外部协议交给系统浏览器。
- 本地代码执行器有输入大小和超时限制，不接受任意 Shell 命令。
