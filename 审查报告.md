# Proofline 当前 Bug、漏洞与代码调试设计

> 复核日期：2026-08-15
>
> 项目：`G:/Codex/xiti`
>
> 范围：React 19 + Vite + Tauri 2 + SQLite 的 Windows 本地优先算法与面试训练应用。
>
> 本文只保留当前源码、当前测试和当前运行状态能够支持的结论。代码调试部分是设计稿，本次不修改源码。

## 1. 当前结论

Proofline 的主流程已经形成：题库/官方平台导入、题面阅读、样例运行、AI 教练、面试练习、计划、错题和知识库。但当前版本仍存在两类必须先处理的问题：

1. 数据安全问题：JSON 导入、同日计划重生成、备份恢复并发可能导致数据丢失或状态损坏。
2. 执行边界问题：C++ 原生执行、AI 请求、远程平台内容和导入链接的信任边界还不够严格。
3. 工程质量问题：E2E 当前有 5 个失败，Rust 格式/静态检查失败，依赖审计存在 7 个漏洞，CI 没有覆盖这些门槛。

建议先发布一个稳定性版本，完成 P0/P1 修复后，再实现代码调试功能。

## 2. 当前验证证据

| 检查项 | 结果 | 说明 |
|---|---|---|
| `npm.cmd test` | 通过 | 33 个测试文件，216 个测试通过 |
| `npm.cmd run build` | 通过但有警告 | `index` 约 1.42 MB、Monaco API 约 3.67 MB、代码运行 worker 约 4.59 MB |
| `cargo test --manifest-path src-tauri/Cargo.toml --all-targets` | 通过 | 63 个 Rust 测试通过 |
| `cargo fmt --manifest-path src-tauri/Cargo.toml --all -- --check` | 失败 | `src-tauri/src/lib.rs` 存在格式差异 |
| `cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings` | 失败 | `backup.rs` 存在 needless borrow，`lib.rs` 存在测试模块位置问题 |
| `npm.cmd run test:e2e` | 失败 | 18 个用例中 13 个通过、5 个失败，集中在样例编辑流程 |
| `npm.cmd audit --audit-level=moderate` | 失败 | 7 个漏洞：4 个高危、3 个中危 |

E2E 的 5 个失败主要有两类原因：运行结果迁移到关闭的 `<dialog>` 后，旧测试仍按主页面查找结果；主样例卡与全文阅读弹窗重复渲染 `.solveExamples`，导致 Playwright strict mode 命中两个节点。这是当前必须修复的 UI 契约/回归门槛问题。

## 3. 当前 Bug 清单

### P0：数据正确性和核心流程

#### BUG-01 JSON 导入可能先清空数据库，再发生崩溃或保存失败

- **位置**：`src-tauri/src/backup.rs:464-472`、`src-tauri/src/db.rs:96-137`、`src/store/useAppStore.ts:1043-1056`
- **现象**：标准导出文件是 `{ manifest, snapshot }` 包装结构。Tauri 的 `import_data` 先把包装对象传入 `save_snapshot`，结构化表会被清空；前端随后才解析并合并。
- **影响**：两步操作之间如果应用崩溃、磁盘写入失败或 SQLite 锁定，用户可能得到空库或半完成导入。
- **修复方式**：后端导入命令只负责文件大小限制、JSON 结构校验和返回快照；前端完成合并后只保存一次。更稳妥的方案是后端在临时数据库中完成合并和校验，成功后原子替换正式数据库。

#### BUG-02 同一天重新生成计划会覆盖已完成记录

- **位置**：`src/lib/planner.ts:75-89`、`src/store/useAppStore.ts:715-738`
- **现象**：计划生成器固定返回 `completedProblemIds: []`；保存时按日期找到旧计划，再用新计划覆盖旧字段。
- **影响**：用户已经完成的题目会重新显示为未完成，今日进度和统计不可信。
- **修复方式**：默认合并旧计划的 `completedProblemIds`，并保留已完成任务；只有用户明确选择“重置今日计划”时才允许清空。生成前增加任务差异预览。

#### BUG-03 样例运行的 stdin 自动识别不可靠

- **位置**：`src/lib/problemRunner.ts:59-99`、`src/lib/problemRunner.ts:367-372`、`src/workers/codeRunner.worker.ts:16-22`
- **现象**：JavaScript 检测到 `process.stdin`、`readline` 或 `input.trim()` 就切换 stdin 模式，但 worker 没有提供 `process`；Python 只要出现 `input(...)` 就跳过函数 harness，自定义辅助函数或注释也可能触发误判。
- **影响**：函数题被当成 stdin 题运行、函数没有被调用，或用户得到误导性的 `process is not defined`；这是核心学习流程的高频误判来源。
- **修复方式**：优先使用题目语言/函数签名决定运行模式；只有用户显式声明标准输入模式时才走 stdin。至少要用 AST 或更严格的顶层调用分析替换正则启发式，并为三种语言补齐 `runProblemSample` 测试。

#### BUG-04 样例输出的字符串引号归一化不一致

- **位置**：`src/lib/problemRunner.ts:779-827`
- **现象**：函数 harness 可能输出 JSON 字符串，例如 `"hello"`；题面期望值可能是裸字符串 `hello`。一侧走 `JSON.parse`，另一侧走空白归一化，内容相同也可能判定失败。
- **修复方式**：先定义统一的输出比较协议：字符串、数字、数组、对象分别解析；无法解析时再做空白和标点归一化。比较结果中保存“原始输出”和“规范化输出”。

### P1：可用性、状态一致性和工程回归

#### BUG-05 样例编辑 E2E 当前有 5 个失败

- **位置**：`src/pages/SolvePage.tsx:1044-1058`、`src/pages/SolvePage.tsx:1306-1357`、`src/pages/SolvePage.tsx:1375-1388`、`e2e/sample-editor.spec.ts`
- **现象**：运行结果位于关闭的结果 dialog 中，旧测试仍按页面正文查找；主卡和全文阅读 dialog 同时存在相同 `.solveExamples` 节点。
- **影响**：关键样例编辑回归无法通过，当前 CI 又没有运行 E2E，因此问题不会阻止合并。
- **修复方式**：给主样例卡、全文阅读样例、运行结果分别设置稳定的 `role` 或 `data-testid`；测试只查询当前打开的 dialog 或指定主卡；保留可访问的运行状态摘要。

#### BUG-06 设置保存失败可能无提示

- **位置**：`src/pages/SettingsPage.tsx:36-43`
- **现象**：`saveAi` 没有复用统一的 `run` 错误处理。普通设置保存成功但凭据写入失败时，可能出现未处理 Promise，页面仍显示成功。
- **修复方式**：将普通设置和凭据保存包装在同一个可回滚事务语义中，统一 busy、错误和成功状态；凭据失败时不清空输入框、不显示成功提示。

#### BUG-07 面试反馈可能写入错误字段

- **位置**：`src/pages/InterviewPracticePage.tsx:168-186`
- **现象**：完成练习时使用“最后一次 coachAction”决定 `coachOutput` 的字段归属。用户先请求“点评回答”，再请求“优化表达”，最后保存时可能把当前文本写错到 `aiFeedback`、`omissions` 或 `improvedAnswer`。
- **修复方式**：每次 AI 输出都保存自己的 `intent`、时间和文本，完成练习时按 intent 聚合，不能依赖最后一次按钮状态。

#### BUG-08 计划数字输入清空后可能传入 NaN

- **位置**：`src/pages/PlanPage.tsx:35-61`、`src/store/useAppStore.ts:247-252`
- **现象**：`type="number"` 输入框清空后调用 `Number('')` 或得到 `NaN`，生成计划路径可能抛出未处理异常。
- **修复方式**：输入状态使用字符串，提交时统一进行 `Number.isFinite`、范围和整数校验；非法值在表单内显示错误，不进入 store。

#### BUG-09 计时练习每秒触发全量持久化

- **位置**：`src/pages/SolvePage.tsx:432-472`、`src/store/useAppStore.ts:171-186`
- **现象**：`seconds` 每秒变化会重新执行自动保存 effect，至少每秒更新一次 attempt，并触发完整 snapshot 写入 SQLite；整个 store 订阅还会带来全局重渲染。
- **影响**：长时间练习时 CPU、磁盘写入和 WebView 重渲染持续升高。
- **修复方式**：将计时器放入 ref；仅在暂停、提交、切题和固定间隔批量保存时写入 duration；页面使用 selector 订阅，避免整个 store 每秒刷新。

### P2：边界和体验问题

#### BUG-10 浏览器预览的 AI 配置可能显示“假保存”

- **位置**：`src/store/useAppStore.ts:367-379`、`src/store/useAppStore.ts:829-840`
- **现象**：浏览器模式的 key 只在模块内存中，`hasAiCredential` 却会写入 localStorage。刷新后 UI 仍显示已配置，真实请求才提示没有密钥；浏览器分支也没有复用后端的 HTTPS/loopback 校验。
- **修复方式**：浏览器模式启动时强制 `hasAiCredential=false`，或明确使用仅当前会话的凭据状态；前后端共享 endpoint 校验，禁止把 Bearer key 发往任意 HTTP 地址。

#### BUG-11 牛客正常子域名可能被白名单拒绝

- **位置**：`src/lib/platform.ts:4-14`、`src-tauri/src/platform.rs:141-165`
- **现象**：白名单缺少常用的 `ac.nowcoder.com`，部分 ACM/竞赛页面无法绑定或导入。
- **修复方式**：前后端使用同一份精确 host 配置，并补充端口、协议、子域名和恶意近似域名测试。

#### BUG-12 手工或导入题目链接存在外链风险

- **位置**：`src/pages/ProblemsPage.tsx:41-45`、`src/pages/SolvePage.tsx:1026-1028`
- **现象**：题目来源只通过字符串包含关系推断平台，`sourceUrl` 保存后直接 `window.open`，没有统一的 HTTPS/官方 host 校验和 `noopener,noreferrer`。
- **修复方式**：导入和手工创建都经过同一个 URL schema 校验；官方平台只允许精确 host；普通外链显示风险提示并使用受控 opener。

#### BUG-13 JS/Python 输出没有硬上限

- **位置**：`src/workers/codeRunner.worker.ts:7-23`、`src/workers/pyodide.worker.ts:36-70`
- **现象**：短时间大量打印会持续增长数组和字符串，当前只有墙钟超时，没有与 C++ 相同的输出上限。
- **修复方式**：统一限制源代码、输入、stdout、stderr 和序列化结果；达到上限立即截断并结束 worker，返回明确的“输出过大”。

## 4. 当前安全与可用性漏洞

### SEC-01 C++ 原生执行缺少沙箱

- **位置**：`src-tauri/src/runner.rs:48-119`
- **风险**：编译后的程序以当前用户权限运行，可以访问本机文件、网络和子进程。超时只调用 `taskkill /T /F`，不能替代完整的资源隔离。
- **处理顺序**：先限制自动执行入口，再加入 Windows Job Object、低权限令牌、临时目录 ACL、禁网和 CPU/内存/文件配额。调试功能必须复用这套边界。

### SEC-02 备份恢复、清空数据与数据库写入没有全局锁

- **位置**：`src-tauri/src/lib.rs:105-109`、`src-tauri/src/backup.rs:147-218`、`src-tauri/src/backup.rs:570-590`
- **风险**：恢复流程会删除 WAL/SHM、重命名数据库和附件目录；并发写入可能造成 `database is locked`、丢写或恢复状态不一致。清空数据时并发写入也可能重新创建被删除的目录。
- **处理方式**：在 `AppState` 增加数据库读写门控；恢复和清空操作独占写锁，先停止后台写入，再完成临时文件校验、fsync、原子替换和重新初始化。

### SEC-03 远程平台数据可触发 panic

- **位置**：`src-tauri/src/platform.rs:515-543`、`src-tauri/src/platform.rs:655-670`
- **风险**：远程 slug 或 HTML href 未通过 `Url::parse` 校验就使用 `expect`。项目使用 `panic = "abort"` 时，异常输入会直接结束进程。
- **处理方式**：所有远程内容转成 `Result<Url, String>`；失败时生成单题失败项并继续批量导入，禁止使用面向远程输入的 `expect`、`unwrap` 和 `unreachable!`。

### SEC-04 JSON/ZIP 导入缺少统一大小和条目限制

- **位置**：`src-tauri/src/backup.rs:147-175`、`src-tauri/src/backup.rs:464-472`
- **风险**：manifest、JSON 文件和 ZIP 内 `xiti.sqlite` 条目没有统一总大小上限；恶意文件可能耗尽内存或磁盘。附件虽有部分限制，但不能覆盖数据库条目。
- **处理方式**：限制源文件大小、解压总大小、数据库条目大小、条目数量、压缩比和 JSON 深度；导入前只读校验，写入临时目录，完成校验后原子替换。

### SEC-05 AI prompt 和响应缺少总量限制

- **位置**：`src/lib/ai.ts:166-190`、`src-tauri/src/ai.rs:267-379`
- **风险**：大题面、历史上下文或超长流式响应造成上下文拒绝、费用失控或内存压力。非 SSE JSON 也缺少响应体上限。
- **处理方式**：前端预估 token，后端按字节再次限制；限制 prompt、非流响应、累计 SSE、单行 SSE 和本地保存文本；超限时取消请求并显示原因。

### SEC-06 CSP 和依赖风险

- **位置**：`src-tauri/tauri.conf.json:28-30`、`package.json:6-7`
- **风险**：CSP 仍允许 `unsafe-eval`、`unsafe-inline` 和宽泛的 `connect-src https:`；依赖审计当前报告 7 个漏洞，包括 React Router、`undici`、`nanoid`、PostCSS 和 DOMPurify。
- **处理方式**：先评估 Monaco 所需权限，尽量收紧 CSP；升级 React Router 至修复版本，逐项验证 `undici`、`nanoid`、PostCSS 和 Monaco/DOMPurify 兼容性，不直接使用未经验证的 `npm audit fix --force`。

### SEC-07 环境变量可能扩大删除范围

- **位置**：`src-tauri/src/lib.rs:22-39`、`src-tauri/src/backup.rs:584-590`
- **风险**：`PROOFLINE_DATA_DIR` 和备份目录可被设置为任意路径，清空数据操作直接 `remove_dir_all`。
- **处理方式**：仅测试构建允许目录覆盖；生产构建校验绝对路径、拒绝盘根和工作区路径，并要求目录内存在应用 marker 后才允许清理。

## 5. 工程和性能风险

1. **SQLite 写入模型**：每个命令新建连接，虽然使用 WAL 和 5 秒 busy timeout，但没有统一写队列，写竞争时会把 `database is locked` 直接暴露给前端。
2. **构建体积**：首屏和 Monaco/worker chunk 较大，应按页面和语言动态加载，并设置 chunk budget。
3. **CI 不完整**：当前工作流只跑 Ubuntu 的 npm 测试和构建，未运行 E2E、Rust fmt/check/test/clippy、npm audit 和 Windows Tauri 打包。
4. **测试盲区**：`runProblemSample` 的 worker 调度和输出比较、备份命令、调试边界、浏览器 AI endpoint、知识库导入归一化仍缺少足够的集成测试。

## 6. 修复优先级

### 0.1.2 稳定性补丁

1. 修复 JSON 导入两阶段清库问题。
2. 修复计划重生成覆盖完成记录。
3. 给恢复、清空和所有数据库写操作增加全局门控。
4. 移除远程 URL 解析路径中的 `expect`/`unwrap`。
5. 修复样例运行模式识别、输出比较和 5 个 E2E 失败。
6. 补齐输入、输出、AI、备份和 ZIP 的大小限制。

### 0.2.0 代码调试基础版

1. 先实现 JavaScript/TypeScript 调试协议和 UI。
2. 接入 Python tracing 和统一变量/调用栈面板。
3. 完成调试记录摘要、资源配额和 E2E 测试。
4. C++ 先提供受限的异常定位降级模式，再根据 Windows 调试器可用性实现断点调试。

### 0.3.0 训练体验增强

1. 计划生成差异预览和可撤销重排。
2. 错题间隔复习与今日任务聚合。
3. AI 调试解释、结果本地缓存和断网重试。
4. 知识点、错题和调试摘要关联。

## 7. 代码调试功能设计

### 7.1 目标和边界

代码调试功能服务于现有的“题面 → 解题函数 → 样例运行 → 错题复盘”闭环。第一阶段只做本地、单题、单次运行的受限调试：

1. 支持 JavaScript/TypeScript、Python、C++17 三种现有语言。
2. 支持断点、继续、单步跳过、单步进入、单步跳出、暂停和终止。
3. 显示当前源位置、调用栈、局部变量、stdout、stderr 和异常信息。
4. 默认使用当前样例输入，允许临时修改输入但不自动覆盖正式样例。
5. 调试代码使用当前编辑器快照；只有点击“应用到草稿”才写回正式代码。
6. AI 只能解释当前暂停点和变量，默认只读，不自动改代码。

第一阶段不做远程调试、真实平台判题调试、任意进程附加、跨题调试和自动修复代码。

### 7.2 用户流程

```text
做题页点击“调试当前样例”
  -> 选择样例或临时输入
  -> 编辑器设置断点
  -> 启动受限调试会话
  -> 命中断点、异常或单步位置
  -> 查看变量、调用栈和输出
  -> 单步、继续、暂停或终止
  -> 查看调试摘要
  -> 可选：保存摘要或应用代码修改
```

桌面端建议在现有代码编辑器下方增加调试抽屉：左侧是控制按钮和调用栈，右侧是变量、输出和异常；移动端改为上下分区，避免新增横向滚动。运行结果 dialog 和调试面板必须使用不同的语义角色，避免再次产生重复选择器问题。

### 7.3 总体架构

```text
SolvePage
  -> DebugSessionStore
  -> DebugSessionService
      -> JavaScript/TypeScript Worker Adapter
      -> Python Pyodide Worker Adapter
      -> Tauri Rust Debug Adapter
          -> 受限 C++ 编译/调试进程
  <- 统一 DebugEvent 事件流
```

前端只发送白名单调试命令，worker 或 Rust adapter 负责执行、暂停、变量快照和资源配额。每个会话都有唯一 `sessionId`，切题、关闭页面、异常和终止时必须释放 worker/子进程。

### 7.4 统一调试协议

```ts
type DebugCommand =
  | { type: 'start'; sessionId: string; code: string; language: string; input: string; breakpoints: number[] }
  | { type: 'continue'; sessionId: string }
  | { type: 'step-over'; sessionId: string }
  | { type: 'step-into'; sessionId: string }
  | { type: 'step-out'; sessionId: string }
  | { type: 'pause'; sessionId: string }
  | { type: 'terminate'; sessionId: string };

type DebugEvent =
  | { type: 'started'; sessionId: string; entryFile: string }
  | { type: 'paused'; sessionId: string; reason: 'breakpoint' | 'exception' | 'step'; location: SourceLocation; stack: StackFrame[]; scopes: Scope[] }
  | { type: 'output'; sessionId: string; stream: 'stdout' | 'stderr'; text: string }
  | { type: 'continued'; sessionId: string }
  | { type: 'completed'; sessionId: string; result: RunCodeResult }
  | { type: 'terminated'; sessionId: string; reason: string }
  | { type: 'error'; sessionId: string; code: string; message: string };
```

协议要求：

- 所有事件必须带 `sessionId`，迟到事件直接丢弃。
- `terminate` 必须幂等，重复终止不能产生未处理异常。
- 事件流断开、worker 重建和进程退出都要生成可解释的 `error` 或 `terminated` 事件。
- 变量快照必须带深度、元素数量和字节上限，避免调试器本身造成内存膨胀。

### 7.5 三种语言的落地方式

| 语言 | 首选方案 | 降级方案和限制 |
|---|---|---|
| JavaScript/TypeScript | 独立 worker + TypeScript source map；在可执行语句位置建立检查点，暂停时返回局部变量 | 首版只支持函数体和常见循环；禁止网络、动态 import、Worker 嵌套；对象序列化限深度 |
| Python | Pyodide worker 使用 `sys.settrace` 或 AST 检查点，返回当前行、locals 和函数栈 | tracing 开销和死循环必须受限；超时后销毁并重建 worker |
| C++17 | 临时工程编译调试信息；通过 Rust adapter 调用受限的 GDB/LLDB MI 协议 | 若 Windows 调试器不可用，先提供编译错误位置、异常位置、调用栈和输出的诊断模式，不开放不受控的调试器进程 |

C++ 调试器不能直接暴露给前端。Rust adapter 负责工具链探测、参数白名单、进程树、Job Object、临时目录和事件转发。

### 7.6 编辑器和调试面板设计

1. Monaco gutter 显示断点，断点状态分为“已设置、已绑定、无效”。
2. 当前暂停行使用单独高亮，不改变用户代码文本。
3. 控制区提供“继续、单步跳过、单步进入、单步跳出、暂停、终止”。运行期间禁用切换题目、语言和样例的操作；终止后恢复。
4. 调用栈按 frame 展开；变量按 Scope 分组，支持展开对象和复制值。
5. 输出区域采用增量追加，超过上限时显示截断原因和累计字节数。
6. 异常区域同时显示语言错误、源文件、行列号和可操作建议。
7. AI 按钮只在暂停状态出现，发送当前题目、当前代码片段、暂停位置和变量摘要，不发送完整历史和密钥。

### 7.7 数据模型

不建议把大量调试事件直接塞进 `Attempt`。建议增加独立摘要和可选快照：

```text
debug_sessions
- id
- attempt_id
- problem_id
- language
- sample_index
- status
- started_at / ended_at
- breakpoint_count
- stop_reason
- summary_json

debug_snapshots
- id
- session_id
- sequence
- file
- line / column
- stack_json
- scopes_json
- stdout_delta / stderr_delta
- created_at
```

默认只保留最近一次会话摘要和少量关键快照；用户明确点击“保存调试记录”后才长期保存。备份中应允许用户选择是否包含调试快照，避免备份文件快速膨胀。

### 7.8 安全和资源配额

调试会话的限制必须比普通样例运行更严格：

- 单次会话最长 30 秒，暂停次数、单步次数和事件总数设上限。
- 源代码、输入、单条输出、累计输出、变量快照和调用栈深度分别设字节或数量上限。
- C++ 调试器、被调程序和所有子进程放入同一个 Job Object；终止时递归结束进程树。
- 禁止网络、任意文件访问、外部命令和动态加载；临时目录使用随机目录并在会话结束清理。
- worker 或调试器崩溃时显示明确原因，不能只显示“运行失败”。
- 不允许通过调试变量、路径或命令参数绕过现有平台 URL、文件和外部进程校验。

### 7.9 测试和发布门槛

实现前先补齐测试契约：

1. 协议单元测试：命令顺序、事件顺序、迟到事件、重复终止和会话切换。
2. JavaScript/TypeScript 测试：断点命中、嵌套函数、异常行、Unicode 输出和大对象截断。
3. Python 测试：`sys.settrace`、死循环、异常、中文变量和 worker 重建。
4. C++ 测试：编译失败、断点不可用、子进程终止、超时、路径隔离和输出限制。
5. UI E2E：1440px、390px、切题中止、关闭调试面板、恢复草稿、保存摘要和 AI 解释入口。
6. 安全测试：路径穿越、网络访问、恶意输入、调试器逃逸和超时清理。
7. 性能测试：首屏不加载调试器；调试模块按语言动态加载；构建包设置 chunk budget。

### 7.10 推荐实施顺序

1. 先修复 BUG-01、BUG-02、BUG-03、BUG-05 和数据库写门控，建立稳定基线。
2. 只实现 JavaScript/TypeScript 调试协议、Monaco 断点和调试面板，验证交互与数据模型。
3. 接入 Python tracing，复用同一变量、调用栈和输出 UI。
4. 完善 C++ 受限诊断模式；确认 Windows 调试器、Job Object 和低权限边界后再开放完整断点调试。
5. 最后接入 AI“解释当前暂停点”，默认只读，且受 prompt 和输出配额保护。

> 本次仅重写本 Markdown 文件；没有修改源码、测试、依赖、构建产物或运行配置。
